# Планировщик задач
Реализован базовый и дополнительный (*) функционал.

Основной функционал:
- создание;
- редактирование;
- удаление;
- повторение;
- отмана задачи.

Дополнительный:
- считывание данных из переменных окружения (TODO_PASSWORD, TODO_PORT, TODO_DBFILE);
- правила повторения ("w ..", "m ..")
- правила повторения ("w ..", "m ..")
- аутентификация;
- Dockerfile.

Запуск:
 - локально:
 ```
TODO_PORT=7540 TODO_DBFILE="scheduler.db" TODO_PASSWORD="12345" go run .
TODO_PORT=7540 TODO_DBFILE="scheduler.db" TODO_PASSWORD="12345" go run .
 ```
 или через сборку исполняемого файла установив соотв-ие переменные окружения.
  
 или через сборку исполняемого файла установив соотв-ие переменные окружения.
  
 - docker:
   ```
    docker build --tag full_planner:v1.1.1 .
    docker run --rm -p 7540:7540 --env-file .env --name full_planner full_planner:v1.1.1
   ```

Примечание:
1. тесты на задние со * "правила повторения ("w ..", "m ..")" не проходят.
Я считаю ошибка в тестах и предложенное мной решение имеет место.
Т.к. в шаге 3 описано задание:
"... 
w <через запятую от 1 до 7> — задача назначается в указанные дни недели, где 1 — понедельник, 7 — воскресенье. Например:
w 7 — задача перенесётся на ближайшее воскресенье;
w 1,4,5 — задача перенесётся на ближайший понедельник, четверг или пятницу;
w 2,3 — задача перенесётся на ближайший вторник или среду.
m <через запятую от 1 до 31, -1, -2> [через запятую от 1 до 12] — задача назначается в указанные дни месяца. При этом вторая последовательность чисел опциональна и указывает на определённые месяцы. Например:
m 4 — задача назначается на четвёртое число каждого месяца;
m 1,15,25 — задача назначается на 1, 15 и 25-е число каждого месяца;
m -1 — задача назначается на последний день месяца;
m -2 — задача назначается на предпоследний день месяца;
m 3 1,3,6 — задача назначается на 3 января, марта и июня;
m 1,-1 2,8 — задача назначается на первый и последний дни февраля и авгуcта.
..."
Например, задача {"20231106", "m 13", "20240213"}. Ожидаемое значение `expected: "20240213"`. Если сегодня 9 декабря 2025 г, "задача назначается на х число каждого месяца"  должны выдать 20260106, исходя из сегоднешней даты.

2. Dockerfile:
Содержимое .env для контейнера:
```
TODO_PORT=7540
TODO_DBFILE=scheduler.db
TODO_PASSWORD=12345
```